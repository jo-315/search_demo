# 環境
flamework: Flask
DB: PostgreSQL

app下のファイルが仮想環境（docker）内のcodeにマウントされる

# 起動コマンド
/devで`docker-compose up`




# dockerコマンド
- 実行
`docker-compose up`

バックグラウンド
`docker-compose up`

- 停止
`docker-compose stop`